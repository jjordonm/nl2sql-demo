"""
function_app – Teams app assets.

Contains the Teams manifest template (teams-manifest/).
The bot logic lives in bot.py at the project root.
The web entry-point is webapp.py (deployed to Azure App Service).
"""
